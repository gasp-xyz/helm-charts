# https://moonrepo.dev/docs/config/workspace
$schema: 'https://moonrepo.dev/schemas/workspace.json'

projects:
  common-deployment: 'charts/common-deployment'
  common-statefulset: 'charts/common-statefulset'
  node: 'charts/node'
  relaychain-testnet: 'charts/relaychain-testnet'

vcs:
  manager: 'git'
  defaultBranch: 'main'
